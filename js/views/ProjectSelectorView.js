/**
 * ProjectSelectorView - Handles project selection UI
 */
class ProjectSelectorView {
    constructor() {
        this.bindEvents();
    }

    bindEvents() {
        // This view is handled by ModalView for now
    }
}

// Create global ProjectSelectorView instance
window.ProjectSelectorView = new ProjectSelectorView();
